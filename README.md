# ai-error-summarizer
Backend service that collects server logs, summarizes error patterns, and suggests root causes using OpenAI GPT. Designed to run as a cron job or webhook with log parsing for distributed system environments.
